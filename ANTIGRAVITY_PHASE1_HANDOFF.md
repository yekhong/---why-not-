# Antigravity IDE 전달용 — WhyNot 1차 보안 격리 코드

## 가장 중요한 지시

이 패키지는 이미 수정된 전체 파일을 포함합니다. 코드를 새로 재해석하거나 UI를 재설계하지 마세요.

다음 행동은 금지합니다.

- `src/index.css` 수정
- 기존 화면 레이아웃, 색상, 여백, 카드 크기 변경
- 테스트용 기본 방 상수와 테스트 ID를 임의 삭제
- 새로운 인증 방식을 또 추가
- 브라우저 Supabase 직접 호출 복구
- 공개 방·외부 공개 투표 기능 복구
- GitHub commit/push
- Vercel 배포
- Supabase 원격 SQL 실행
- `.env` 값 출력

## 해야 할 일

1. 현재 프로젝트를 별도 로컬 백업합니다.
2. 이 패키지의 아래 파일을 동일 경로에 적용합니다.

```text
server.ts
src/App.tsx
src/types.ts
supabase_migration_p0_bff_forward.sql
supabase_migration_p0_bff_rollback.sql
WHYNOT_THREE_STAGE_IMPLEMENTATION.md
```

3. SQL 파일은 실행하지 말고 검토만 합니다.
4. 다음 명령만 로컬에서 실행합니다.

```bash
npm ci --ignore-scripts
npm run lint
npm run build:server
npm run build
```

5. 실패하면 임의 우회하지 말고 오류 원문, 명령, 파일 위치를 보고합니다.

## 1차 구현에서 변경된 보안 구조

### 인증

- 비밀번호를 브라우저에 저장하지 않습니다.
- 신규 비밀번호는 랜덤 salt가 포함된 `scrypt` 해시로 저장합니다.
- 기존 SHA-256 계정은 로그인 성공 시 `scrypt`로 점진 변환합니다.
- 로그인 성공 시 256비트 임의 토큰을 발급하고 브라우저에는 `HttpOnly`, `SameSite=Strict` 쿠키만 저장합니다.
- 운영 환경 쿠키에는 `Secure`가 적용됩니다.
- 세션 원문은 DB에 저장하지 않고 SHA-256 토큰 해시만 저장합니다.
- 로그아웃과 계정 복구 시 기존 세션을 무효화합니다.
- 로그인·가입·복구에는 속도 제한을 적용합니다.

### 권한

- 클라이언트가 보내는 `userId`, `hostId`, `evaluatorId`, `authorId` 등을 신원으로 신뢰하지 않습니다.
- 서버가 쿠키 세션에서 사용자 ID를 결정합니다.
- 회의실 조회·수정은 참여자만 가능합니다.
- 방 설정·단계 전환·초대 관리·종료는 방장만 가능합니다.
- 임의 단계 건너뛰기를 서버 상태 전이표로 차단합니다.
- 유효한 초대 링크 없이 직접 참여하는 API를 차단합니다.
- 타인의 아이디어 수정·삭제를 차단합니다.
- 제출 단계가 끝난 후 아이디어 수정·삭제를 차단합니다.
- 클라이언트가 아이디어 ID를 지정해 기존 행을 덮어쓰는 경로를 제거했습니다.

### 데이터 접근

- `src/App.tsx`의 `supabase.from`, `supabase.rpc`, `supabase.channel` 직접 호출을 제거했습니다.
- 프론트는 Express BFF API만 사용합니다.
- 실시간 갱신은 3초 BFF 폴링으로 대체합니다.
- 운영 환경은 `SUPABASE_URL`과 서버 전용 `SUPABASE_SERVICE_ROLE_KEY`가 없으면 시작하지 않습니다.
- 서비스 역할 키를 `VITE_` 환경 변수나 브라우저 코드에서 사용하면 안 됩니다.

### 공개·개발 기능 차단

- 모든 신규 방은 비공개로 저장합니다.
- 공개 검색과 외부 공개 투표 컨트롤을 제거했습니다.
- 데모 seed, 평가 seed, 별 투표 seed API를 404로 차단합니다.
- 직접 참여 API를 차단합니다.
- 테스트용 기본 데이터 함수와 상수는 삭제하지 않았지만 운영 경로에서 실행되지 않습니다.

### 입력 방어

- JSON 요청 크기를 100KB로 제한합니다.
- 교차 출처의 쿠키 기반 변경 요청을 차단합니다.
- 참고 링크는 `http` 또는 `https`만 허용합니다.
- 제목·설명·태그·파일명 길이를 제한합니다.
- 기본 보안 헤더와 API `no-store`를 적용합니다.

## SQL 적용 전 필수 확인

`supabase_migration_p0_bff_forward.sql`은 원격에서 자동 실행하면 안 됩니다.

사전 확인 항목:

- `public.user_accounts.id`가 UUID인지
- `room_invites`가 존재하는지
- Supabase 서버 환경에 서비스 역할 키가 설정되어 있는지
- 현재 정책·권한·함수·뷰를 별도 파일로 백업했는지
- 기존 활성 초대가 모두 무효화되어도 되는지

이 SQL은 브라우저 역할(`anon`, `authenticated`)의 public 테이블·뷰·함수 권한을 제거하고 BFF만 DB를 사용하도록 만듭니다. 기존 허용 정책을 모두 제거하므로 반드시 스테이징에서 먼저 검증해야 합니다.

`supabase_migration_p0_bff_rollback.sql`은 위험한 공개 정책을 자동 복원하지 않습니다. 완전 복원은 검토된 DB 백업으로 해야 합니다.

## 검증 결과

로컬에서 확인한 결과:

- `npm run lint`: 통과
- `npm run build:server`: 통과
- `npm run build`: 통과
- 비로그인 회의실 조회: 401
- 약한 비밀번호 가입: 400
- 정상 가입 및 HttpOnly 세션: 성공
- 세션 복원: 성공
- 다른 Origin의 변경 요청: 403
- 외부인의 비공개 방 조회·단계 변경: 403
- 위조 `hostId`: 403
- 허용되지 않은 단계 건너뛰기: 409
- 직접 방 참여: 403
- 데모 데이터 생성: 404
- `javascript:` 참고 링크: 400
- 클라이언트 지정 아이디어 ID: 무시하고 서버 UUID 발급
- 로그아웃 후 세션 조회: 401

## 아직 구현하지 않은 것

다음은 2차 또는 3차 작업이며 이번 파일을 적용했다고 완료 처리하면 안 됩니다.

- 원문과 AI 보완안 비교·승인 모달
- 전원 제출 전 아이디어·기준 비공개 및 동시 공개
- 기준 세트 80% 합의
- 기준별 `MET/PARTIAL/NOT_MET/UNSURE`
- 중간 결과 완전 비공개
- 빠른 결정 모드
- AI 최종 리포트 DB 스냅샷
- 라운드 이력 보존
- 원격 DB 마이그레이션 및 배포

## Antigravity가 반환할 보고서

다음 형식으로만 결과를 보고하세요.

1. 적용한 파일 목록
2. `src/index.css` 변경 여부와 SHA-256
3. 실행한 명령과 종료 코드
4. lint/build 결과
5. 원격 작업을 하지 않았다는 확인
6. 남은 오류와 미구현 범위

