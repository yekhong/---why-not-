# Antigravity IDE용 WhyNot 2차 코드 적용 지시서

## 작업 목표

첨부된 2차 구현 코드를 현재 WhyNot 로컬 프로젝트에 반영하세요.

이 작업은 화면을 새로 디자인하는 작업이 아닙니다. 기존 UI와 핵심 기능은 유지하고, 아래 기능과 처리 로직만 추가하는 작업입니다.

## 가장 중요한 원칙

1. AI는 아이디어를 평가하거나 최종 결론을 내리지 않습니다.
2. AI는 사용자의 표현을 중립적으로 정리하고 검토 질문을 제안합니다.
3. AI 결과는 작성자가 승인해야만 원문에 적용됩니다.
4. 다른 사람의 의견을 보기 전에 각자 먼저 작성합니다.
5. 평가 기준은 방장이 단독 확정하지 않고 참여자가 동의합니다.
6. 중간 평가 결과는 모든 평가가 끝나기 전에 공개하지 않습니다.
7. 기존 완료 방에는 새로운 점수 공식을 적용하지 않습니다.

## 제공 파일

다음 파일은 설명용 예시가 아니라 실제 수정 코드입니다.

- `server.ts`
- `src/App.tsx`
- `src/types.ts`
- `supabase_migration_phase2_persona_forward.sql`
- `supabase_migration_phase2_persona_rollback.sql`

## 정확한 적용 방법

### 1단계: 백업

현재 프로젝트 전체를 복사하거나 로컬 브랜치를 생성하세요.

현재 파일을 바로 삭제한 뒤 다시 작성하지 마세요. 반드시 백업 후 진행하세요.

### 2단계: 코드 파일 반영

전달받은 다음 세 파일을 동일 경로에 반영하세요.

| 전달 파일 | 적용 위치 |
|---|---|
| `server.ts` | 프로젝트 루트의 `server.ts` |
| `App.tsx` | `src/App.tsx` |
| `types.ts` | `src/types.ts` |

전달 파일의 기능을 다시 추측하여 별도로 재작성하지 마세요. 전달된 코드 자체를 기준으로 반영하세요.

### 3단계: SQL 파일 보관

다음 SQL 파일은 프로젝트 루트에 추가만 하세요.

- `supabase_migration_phase2_persona_forward.sql`
- `supabase_migration_phase2_persona_rollback.sql`

이번 단계에서는 원격 Supabase에 실행하지 마세요.

## 코드에 구현된 기능

### A. AI 표현 보완

`AI 아이디어 디벨롭` 요청 결과를 원문에 즉시 덮어쓰지 않습니다.

화면에 다음 세 가지를 표시합니다.

- 작성자가 입력한 원문
- AI가 제안한 보완문
- AI가 제안한 검토 질문

작성자가 `승인하고 적용`을 눌렀을 때만 보완문을 입력창에 적용합니다.

`원문 유지`를 누르면 원문을 그대로 둡니다.

### B. 평가 기준 제안 동시 공개

참여자는 다른 사람의 기준을 보기 전에 자기 기준부터 제안합니다.

`기준 제안 완료하기`를 누르면 해당 사용자의 완료 상태를 저장합니다.

단계 시작 시점에 포함된 참여자 전원이 완료하기 전에는 다음 정보를 숨깁니다.

- 다른 사람이 작성한 기준
- 다른 사람의 작성자 식별값
- AI 기준 통합 시작 기능

전원이 완료하면 기준 제안을 작성자 정보 없이 동시에 공개합니다.

### C. 평가 기준 80% 동의

방장의 단독 기준 확정 기능을 사용하지 않습니다.

각 참여자는 다음 중 하나를 선택합니다.

- `이 기준으로 평가 진행`
- `보완 필요`

고정된 참여 대상의 80% 이상이 동의하면 평가 단계로 이동합니다.

예시는 다음과 같습니다.

| 참여 대상 | 승인에 필요한 인원 |
|---:|---:|
| 2명 | 2명 |
| 3명 | 3명 |
| 4명 | 4명 |
| 5명 | 4명 |
| 6명 | 5명 |

방장의 표도 다른 사용자와 같은 한 표입니다.

### D. 모든 아이디어의 모든 기준 평가

각 아이디어마다 확정된 모든 평가 기준에 다음 값 중 하나를 입력합니다.

- `MET`: 충족
- `PARTIAL`: 일부 충족
- `NOT_MET`: 미충족
- `UNSURE`: 잘 모르겠음

아이디어 하나 또는 기준 하나라도 누락되면 프론트엔드와 서버 모두 제출을 거부해야 합니다.

### E. 결과 지표 분리

기준 충족도와 추천 표를 한 점수로 합치지 않습니다.

다음 정보를 각각 표시합니다.

- 기준 충족도
- 유효 응답 수
- `잘 모르겠음` 응답 수와 비율
- 추천 표 수
- 보류 표 수
- 우려 표 수

`UNSURE`는 기준 충족도 계산의 분모에서 제외하지만, 숨기지 않고 별도 비율로 공개합니다.

### F. 기존 결과 보호

- 기존 완료 방: `engine_version = 1`
- 마이그레이션 이후 신규 방: `engine_version = 2`

`engine_version = 1`인 완료 방의 점수와 결과는 새로운 공식으로 다시 계산하지 않습니다.

## 변경하면 안 되는 항목

다음 항목은 수정하지 마세요.

- `src/index.css`
- 기존 색상
- 기존 화면 배치
- 기존 카드 크기와 스타일
- 기존 로그인·로비·방 생성 화면 디자인
- 룰렛 기능
- 별 스티커 최종 투표
- 테스트용 기본 방 데이터
- 테스트용 사용자 ID

새 기능을 표시하기 위해 필요한 최소 UI만 기존 컴포넌트 내부에 추가하세요.

## 보안 규칙

1. 클라이언트가 보낸 `userId`, `hostId`, `evaluatorId`만으로 신원을 판단하지 마세요.
2. 인증 미들웨어가 확인한 사용자만 변경 API를 사용할 수 있어야 합니다.
3. 서버가 아이디어 ID, 기준 ID, 참여자 자격을 다시 검증해야 합니다.
4. AI API 키와 Supabase 서비스 키를 프론트엔드에 넣지 마세요.
5. `.env`, 토큰, 비밀번호, API 키를 출력하지 마세요.
6. 원격 GitHub, Supabase, Vercel을 변경하지 마세요.

## 이번 작업에서 하지 않을 일

사용자 요청에 따라 지금은 다음 검증을 실행하지 않습니다.

- 빌드
- 린트
- 자동 테스트
- 브라우저 테스트
- 원격 DB 마이그레이션
- 배포

코드 반영을 완료하면 임의로 다음 작업을 진행하지 말고, 변경 파일 목록만 보고한 뒤 멈추세요.

## 완료 보고 형식

다음 형식으로만 보고하세요.

```text
1. 변경한 파일
2. 추가한 기능
3. 변경하지 않은 기존 UI
4. 실행하지 않은 SQL
5. 아직 검증하지 않은 항목
6. 원격 변경 여부
```

`완벽히 검증됨`, `100% 안전함`, `오류 없음`처럼 실제 검증 없이 확정하는 표현을 사용하지 마세요.
