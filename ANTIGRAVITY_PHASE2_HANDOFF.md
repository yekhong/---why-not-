# WhyNot 2차 구현 전달서

## 목적

기존 WhyNot UI와 핵심 흐름은 유지하면서 다음 원칙을 코드로 추가했습니다.

> AI는 심판이 아니라 통역자이자 정리자입니다.

> 누가 말했는지가 아니라, 무엇을 말했는지로 결정합니다.

## 구현 내용

### 1. AI 아이디어 표현 보완 승인

- 원문과 AI 보완안을 함께 보여줍니다.
- 최대 3개의 검토 질문을 함께 표시합니다.
- 작성자가 `승인하고 적용`을 눌러야 입력창에 반영됩니다.
- `원문 유지`를 선택하면 원문은 변경되지 않습니다.

### 2. 평가 기준 제안 동시 공개

- 다른 사람의 기준 제안을 보기 전에 자기 제안을 먼저 작성합니다.
- `기준 제안 완료하기`로 개인 완료 상태를 저장합니다.
- 단계 시작 시점의 참여자 전원이 완료하기 전까지 타인의 제안은 숨깁니다.
- 전원이 완료하면 익명 제안을 동시에 공개합니다.
- 동시 공개 전에는 AI 기준 정리를 시작할 수 없습니다.

### 3. 평가 기준 팀 동의

- 방장 단독 확정 UI를 제거했습니다.
- 모든 참여자가 `이 기준으로 평가 진행` 또는 `보완 필요`를 선택합니다.
- 고정된 참여 대상의 80%가 동의하면 자동으로 평가 단계로 이동합니다.
- 방장도 다른 참여자와 동일한 한 표만 가집니다.

### 4. 구조화된 공통 기준 평가

각 아이디어의 모든 확정 기준을 다음 네 값 중 하나로 평가합니다.

- `MET`: 충족
- `PARTIAL`: 일부 충족
- `NOT_MET`: 미충족
- `UNSURE`: 잘 모르겠음

일부 아이디어 또는 일부 기준만 제출하는 요청은 서버가 거부합니다.

### 5. 결과 지표 분리

- 기준 충족도와 추천·우려 표를 합친 단일 점수를 만들지 않습니다.
- `UNSURE`는 비율로 표시하며 충족도 계산 분모에서 제외합니다.
- 기준 충족도, 유효 응답 수, UNSURE 비율, 추천·우려 표 수를 분리합니다.
- 평가 완료 전 집계를 공개하지 않는 기존 게이트를 유지합니다.

### 6. 과거 완료 결과 보호

- `engine_version = 1` 완료 방은 기존 결과를 유지합니다.
- 마이그레이션 이후 신규 방은 `engine_version = 2`를 사용합니다.
- 완료된 방에는 새 계산식을 소급 적용하지 않습니다.

## 변경 파일

- `server.ts`
- `src/App.tsx`
- `src/types.ts`
- `supabase_migration_phase2_persona_forward.sql`
- `supabase_migration_phase2_persona_rollback.sql`

## 변경하지 않은 항목

- `src/index.css`
- 기존 색상, 배치, 카드 스타일
- 테스트용 기본 방 및 테스트용 ID
- 기존 핵심 단계와 룰렛/별 투표 기능
- 원격 GitHub, Vercel, Supabase

## Antigravity IDE 적용 순서

1. 현재 프로젝트를 별도 로컬 브랜치 또는 복사본으로 백업합니다.
2. 전달 파일을 같은 경로에 반영합니다.
3. SQL은 원격에서 실행하지 않습니다.
4. 로컬 Supabase 또는 별도 테스트 프로젝트에서만 forward SQL을 검토·적용합니다.
5. `.env`와 비밀키 파일은 복사하거나 출력하지 않습니다.
6. 반영 후에도 배포하지 말고 로컬에서만 확인합니다.

## 아직 검증하지 않은 항목

사용자 요청에 따라 이번 단계에서는 빌드, 린트, 자동 테스트, 브라우저 시나리오 검증을 실행하지 않았습니다.

따라서 “구현 완료”는 코드 작성 완료를 뜻하며 “검증 완료”를 뜻하지 않습니다.

다음 단계에서 확인할 항목:

1. TypeScript 빌드
2. 기존 UI 시각 회귀
3. 2명·3명·6명 기준 동의 임계값
4. 마지막 참여자 완료 전후 제안 공개 범위
5. N개 아이디어 × M개 기준 누락 제출 차단
6. UNSURE 분모 제외 계산
7. 기존 완료 방 engine v1 결과 불변
8. 마이그레이션 forward/rollback

## 주의

`supabase_migration_phase2_persona_forward.sql`이 적용되지 않은 DB에서는 신규 컬럼과 테이블을 사용하는 영구 저장 기능이 동작하지 않습니다. 원격 DB에는 별도 승인 없이 실행하지 마세요.
