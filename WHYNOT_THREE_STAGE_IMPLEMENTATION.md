# WhyNot 3분할 구현 현황

이 문서는 기존 UI를 유지하면서 WhyNot을 세 번에 나누어 안전하게 구현하기 위한 기준 문서입니다.

## 공통 원칙

- `src/index.css`와 기존 화면의 스타일·레이아웃은 변경하지 않습니다.
- 기존 핵심 단계와 기능은 유지합니다.
- 보안을 이유로 기능을 거짓으로 성공 처리하지 않습니다.
- AI는 심판이 아니라 통역자·정리자로만 작동합니다.
- 각 단계는 로컬에서 검증한 후 다음 단계로 넘어갑니다.
- GitHub push, Vercel 배포, Supabase 원격 SQL 실행은 별도 승인 전까지 하지 않습니다.

## 1차: 인증·권한·데이터 접근 격리

상태: **로컬 구현 및 검증 완료**

목표:

- 평문 비밀번호·브라우저 신원 신뢰 제거
- HttpOnly 쿠키 세션 도입
- 모든 회의실 API를 로그인 및 참여자 권한으로 보호
- 브라우저의 Supabase 직접 조회·수정·Realtime 구독 제거
- BFF(Express API) 단일 경로로 데이터 접근 통일
- 운영용 공개 방·공개 투표·강제 데이터 생성 경로 차단
- 기존 UI 스타일 유지

주요 파일:

- `server.ts`
- `src/App.tsx`
- `src/types.ts`
- `supabase_migration_p0_bff_forward.sql`
- `supabase_migration_p0_bff_rollback.sql`

## 2차: 페르소나 중심 의견 수집·평가

상태: **코드 작성 완료, 검증 보류**

목표:

- AI 보완 결과를 원문과 나란히 비교하고 작성자가 승인할 때만 반영
- AI가 중립적 표현과 검토 질문만 제안하도록 제한
- 각자 제출을 마치기 전에는 타인의 아이디어와 기준 제안을 숨김
- 전원 완료 또는 마감 때 동시에 공개
- 작성자 정보는 평가 종료 전까지 화면에서 비공개
- 모든 후보를 동일한 카드 형식으로 표시
- 모든 후보와 확정 기준을 빠짐없이 평가한 경우에만 일괄 제출
- 중간 집계 비공개
- 기준 충족도, 유효 응답 수, `UNSURE` 비율, 추천·우려 표를 분리 표시

필요한 추가 DB 설계:

- 단계별 참여자 스냅샷
- 공개 시점 및 단계 완료 기록
- 기준별 구조화 평가
- 기준 세트 승인 투표
- 엔진 버전

## 3차: 빠른 결정·최종 근거 리포트·통합 검증

상태: **아직 구현하지 않음**

목표:

- 빠른 결정 모드: 선택지 등록 → 비공개 개별 투표 → 동시 결과 공개
- 심층 결정과 빠른 결정을 방 생성 시 명확히 분리
- 최종 결과에 선정 이유, 주요 우려, 미확인 가정, 다음 검증 과제 표시
- AI 리포트 원문·입력 스냅샷·모델·프롬프트 버전을 DB에 고정 저장
- 재검토 시 기존 결과를 덮어쓰지 않는 라운드 이력
- 보안·권한·익명성·회귀·접근성 통합 테스트

## UI 보존 증거

1차 작업 전후 `src/index.css` SHA-256:

```text
8a760a784f79570e06fdcb0c6a2677ea66c304230b7925ea313d63b46d8ba407
```

스타일시트는 변경하지 않았습니다. 보안상 사용할 수 없는 공개 투표 및 개발용 강제 조작 컨트롤만 제거했습니다.
