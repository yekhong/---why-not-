# PRD v0.2 플로우 변경 구현 계획서

`PRD.md` (v0.2) 스펙을 분석한 결과, 현재 기존 구현 코드와 차이가 있는 핵심 플로우 차이점들과 그에 따른 단계별 구현 계획을 정리했습니다.

---

## 🔍 PRD v0.2 vs 현재 구현 비교 분석

| 구분 | 기존 구현 | PRD v0.2 명세 | 변경할 내용 |
|---|---|---|---|
| **역할 및 접근 제어** | 방장/참여자 구분 모호, 비로그인시 랜딩 접근 일부 가능 | **로그인 필수**, **방장(Host)** / **참여자(Member: 최대6명)** / **투표자(Voter: 최대30명)** 로 역할 명확 분리 | URL 파라미터(`?role=voter`) 및 계정 권한 체크. 투표자는 2차 투표 단계 전까지 **대기 화면**만 노출 |
| **1단계: 아이디어 등록** | 실명/익명 혼재 | **100% 익명**, 참여자별 [등록 완료] 체크 후 **전원 완료 시 방장에게 [다음 단계로] 활성화** | 참여자별 완료 상태 DB 관리, 방장이 수동으로 다음 단계 전환 |
| **2단계: 기준 제안 및 확정** | 방장이 수동으로 기준을 리뷰하고 추가/삭제하여 확정 | **참여자별 기준 1~3개 제안** (직접/AI) → **AI가 중복 제거 & 유사 항목 자동 병합** 후 자동 확정 (방장 수정 없음) | 방장 수동 수정 단계 제거, AI 자동 병합 API 연동 |
| **3단계: 1차 찬반 투표** | 득표수 표출 | 찬성/반대 + 기준 1개 이상 필수 선택 + **사유 작성 필수 (익명)** | 사유 및 기준 체크 유효성 검사 강화 |
| **4단계: 2차 소거** | 단순 득표 소거 | **규칙 기반(비-AI) 정렬 & 컷 라인 계산**<br>① 찬성표 많은 순 → ② 찬반 팽팽한 순 → ③ 반대표 적은 순<br>컷 라인: `max(ceil(아이디어 수 × 0.6), 최종 결과 수 + 1)` | 백엔드/클라이언트 결정론적 정렬 알고리즘 구현 |
| **5단계: 2차 투표** | 별도의 투표 없음 또는 소거만 수행 | **참여자 + 투표자 (최대30명)** 동시 참여<br>후보 중 N개(최종 결과 수) 순위(1~N위) 지정 → **보르다 점수 합산** | 순위 지정 별 스티커 배분 UI 및 보르다(Borda) 점수 합산 로직 |
| **6단계: 최종 후보 표시** | 자동 투표 종료 | **상위 N개 자동 표시** (별도 최종 투표 없음), 동점 시 **[게임하기] (룰렛 등, 선택)** | 상위 N개 자동 출력 & 동률 시 룰렛 미니게임 버튼 노출 |
| **7단계: 최종 리포트** | 기본 AI 요약 | **AI 근거 요약 (찬반 사유 재구성) & PDF 다운로드** | 사유 텍스트 AI 클러스터링/재구성 API 및 PDF 출력 |

---

## 🛠️ 변경 계획 상세

### 1. Data Schema & Types (`src/types.ts`, Supabase DB)
- `Room` 및 `RoomDetails` 인터페이스 업데이트
- `Participant` 테이블/타입에 `role` ('HOST' | 'MEMBER' | 'VOTER'), `isIdeaDone` (아이디어 등록 완료 여부) 추가
- `Evaluation` 타입에 `reasonText` 필수화
- `RankedVote` (2차 투표 순위 투표) 스키마 추가: `{ ideaId, rank, score }`

### 2. 컴포넌트 & 플로우 구현 (`src/App.tsx`)
- **역할 분기 (Voter 대기 화면)**:
  - `role === 'voter'` 접속 시, 방 상태가 `EVALUATION_ROUND_2` (2차 투표) 이전이면 "2차 투표 대기 중" 안내 화면 표시
- **아이디어 등록 전원 완료 체크**:
  - 각 참여자가 [아이디어 등록 완료] 버튼을 누름 → 전원 완료 시 방장 화면에만 [다음 단계로] 버튼 활성화
- **AI 자동 기준 병합 (CRITERIA_PROPOSAL -> CRITERIA_REVIEW 생략/자동전환)**:
  - 참여자들이 등록한 1~3개 기준들을 Gemini AI로 보내 중복 제거 후 바로 확정
- **규칙 기반 2차 소거 정렬 알고리즘**:
  - 1차 찬반 표집계 -> `(찬성수 - 반대수)`, `찬반비율 팽팽함`, `반대표적음` 기준 소거
- **2차 투표 (별 스티커 / 보르다 순위 투표)**:
  - 1~N위 순위 지정 Drag/Select UI 제공 및 점수 합산
- **최종 결과 & 룰렛 게임**:
  - 결과 자동 표출 및 동점 발생 시 룰렛 게임 팝업

---

## 🧪 검증 계획

1. **자동 검증**: `npx tsc --noEmit`을 실행하여 TypeScript 타입 일치 여부 확인
2. **수동 검증**: 
   - 방장/참여자/투표자 계정 시뮬레이션을 통해 각 단계별 (아이디어 완료 → AI 기준 확정 → 1차 찬반 → 2차 소거 → 2차 투표 → 최종 결과) 전달 흐름 동작 테스트
   예시 (최종 결과 수가 3개일 때): 1차 소거를 통과해 남은 후보 아이디어들 중에서, 각 투표자(참여자 + 최대 30명 투표자)가 가장 마음에 드는 순서대로 1위, 2위, 3위를 선택합니다.
    1위로 선택받으면 ➡️ 3점 부여
    2위로 선택받으면 ➡️ 2점 부여
    3위로 선택받으면 ➡️ 1점 부여
  모든 사람의 순위 점수를 합산해 가장 높은 점수를 받은 상위 아이디어들이 최종 선택되는 방식입니다. (UI에서는 1위~3위 선택 또는 별 스티커 갯수로 쉽게 투표하도록 구성됩니다.)
